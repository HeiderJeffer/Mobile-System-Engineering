<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dellxps15.roomwordsample2">
//  Google login add com.example.dellxps15.roomwordsample2 to Start Integrating Google Sign-In into Your Android App.
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="Heider Fountain Pen Store" android:name=".CheckoutActivity"/>
        <activity android:label="Heider Fountain Pen Store" android:name=".PaymentActivity"/>
        <activity android:label="Heider Fountain Pen Store" android:name=".SuccessActivity"/>
        <activity android:label="Heider Fountain Pen Store" android:name=".DashboardActivity"/>
        <activity android:label="Heider Fountain Pen Store" android:name=".LoginActivity"/>
        <activity android:label="Heider Fountain Pen Store" android:name=".RegisterActivity"/>
    </application>

</manifest>